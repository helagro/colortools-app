<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">

    <dist:module dist:instant="true" />

    <application
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="se.helagro.colorcompare.MyApp"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="se.helagro.colorcompare.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:launchMode= "singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="se.helagro.colorcompare.MainActivity"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden"
            android:launchMode= "singleTask"
            >
        </activity>
        <activity android:name="se.helagro.colorcompare.LicensesActivity"
            android:launchMode= "singleTask"/>

    </application>

</manifest>